namespace distream
{
    class Connection
    {
    private:
        // list of guilds
        // guild contains a link to a threaded player

    public:
        Connection(/* args */);
        ~Connection();
    };
    
}